# Contributing

1. Read `AGENTS.md` and `docs/architecture/overview.md`.
2. Create a branch from `main`.
3. Add or update tests before changing behavior.
4. Run `npm run check`.
5. Keep generated videos out of Git; publish them as release artifacts or store them in object storage.
6. Update the wiki index and changelog when behavior or architecture changes.
